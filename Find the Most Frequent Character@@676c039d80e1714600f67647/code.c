// Your code here...
#include <stdio.h>
#include <string.h>
int main(){
    char s[50];
    fgets(s,sizeof(s),stdin);
    for(int i=0;i,strlen(s);i++){
        if !(strchr(st,s[i])){
            for( int j=i+1;j<strlen(s);j++){
                
            }
        }
    }
    return 0;
}