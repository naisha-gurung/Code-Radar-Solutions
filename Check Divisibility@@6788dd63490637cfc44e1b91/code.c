#include <stdio.h>
int main() {
    int n;
    scanf("%d",&n);
    if(n%5==0 || n%11==0){
        char st[50]="Divisible"
        printf("s",st);
        return 0;
    }
    char st[50]="Not Divisible";
    printf("%s",st);
    return 0;
}