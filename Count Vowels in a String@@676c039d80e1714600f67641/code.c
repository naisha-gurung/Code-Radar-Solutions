#include <stdio.h>
#include <string.h>
int main() {
    char ch[50];
    scanf("%s",&ch);
    int n=strlen(ch);
    int count=0;
    for(int=0;i<n;i++){
        // if(ch[i]=='a'|| ch[i]=='e' || ch[i]=='i' || ch[i]=='o' || ch[i]=='u' || ch[i]=='A' || ch[i]=='E'|| ch[i]=='I'|| ch[i]=='O' || ch[i]=='U'){
        //     ++count;
        // }
    }
    // printf("%d",count);
    return 0;
}